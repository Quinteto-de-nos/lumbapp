<Page x:Class="LumbApp.GUI.IngresoDatosPracticante"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:LumbApp.GUI" 
      xmlns:valRule="clr-namespace:LumbApp.GUI.UserDataValidations" 
      mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="800"
      Title="LumbApp">
    <Page.Resources>
        <local:DatosPracticante x:Key="Ods"/>
        
        <ControlTemplate x:Key="ValidationTemplate">
            <DockPanel>
                <TextBlock Foreground="Red" FontSize="20">!</TextBlock>
                <AdornedElementPlaceholder/>
            </DockPanel>
        </ControlTemplate>

        <Style x:Key="TextBoxInError" TargetType="{x:Type TextBox}">
            <Style.Triggers>
                <Trigger Property="Validation.HasError" Value="true">
                    <Setter Property="ToolTip"
              Value="{Binding RelativeSource={x:Static RelativeSource.Self},
                              Path=(Validation.Errors)[0].ErrorContent}"/>
                </Trigger>
            </Style.Triggers>
        </Style>
    </Page.Resources>
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition/>
            <RowDefinition/>
            <RowDefinition />
            <RowDefinition />
            <RowDefinition />
            <RowDefinition />
            <RowDefinition />
            <RowDefinition />
            <RowDefinition />
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition/>
        </Grid.ColumnDefinitions>
        <Border  Grid.Row="0" Height="35" Width="800" Padding="5" Background="#4E87D4" HorizontalAlignment="Center" VerticalAlignment="Top" >
            <Label VerticalAlignment="Center" HorizontalAlignment="Center"  Foreground="White">Ingrese sus datos</Label>
        </Border>

        <!--Nombre-->
        <Border Grid.Row="1" Height="30" Width="500" Background="#91c0ff" HorizontalAlignment="Center" VerticalAlignment="Bottom" >
            <Label VerticalAlignment="Center" HorizontalAlignment="Center" FontSize="14" Foreground="White">Ingrese su Nombre</Label>
        </Border>

        <TextBox Grid.Row="2"  Height="35" Width="500" HorizontalAlignment="Center" VerticalAlignment="Bottom" Name="Nombre" />
        
        <!--Apellido-->
        <Border Grid.Row="3" Height="30" Width="500" Background="#91c0ff" HorizontalAlignment="Center" VerticalAlignment="Bottom" >
            <Label VerticalAlignment="Center" HorizontalAlignment="Center" FontSize="14" Foreground="White">Ingrese su Apellido</Label>
        </Border>

        <TextBox Grid.Row="4"  Height="35" Width="500"  VerticalAlignment="Center" HorizontalAlignment="Center" Name="Apellido" />

        <Border Grid.Row="5" Height="30" Width="500"  Background="#91c0ff" HorizontalAlignment="Center" VerticalAlignment="Bottom">
            <Label VerticalAlignment="Center" HorizontalAlignment="Center" FontSize="14" Foreground="White">Ingrese su DNI</Label>
        </Border>
        
        <TextBox Grid.Row="6" Name="dni"  Height="35" Width="500"  FontSize="15"  VerticalAlignment="Center" 
         Validation.ErrorTemplate="{StaticResource ValidationTemplate}"
         Style="{StaticResource TextBoxInError}">
            <TextBox.Text>
                <Binding Path="Dni" Source="{StaticResource Ods}"  UpdateSourceTrigger="PropertyChanged" >
                    <Binding.ValidationRules>
                        <valRule:DniRangeRule Min="1000000" Max="999999999"/>
                    </Binding.ValidationRules>
                </Binding>
            </TextBox.Text>
        </TextBox>

        <Button Grid.Row="7" Grid.Column="0" Height="35" Width="200" Padding="5" HorizontalAlignment="Center" VerticalAlignment="Bottom"
                Background="#4E87D5" Foreground="White" FontSize="18" FontWeight="Bold"
          Name="IniciarSimulacion" Click="IniciarSimulacion_Click">
            Iniciar Simulación
        </Button>

    </Grid>
</Page>
